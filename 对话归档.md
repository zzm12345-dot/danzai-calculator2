# 蛋仔派对充值折扣计算器 - 开发对话归档

## 一、项目概述
本项目是一个蛋仔派对充值折扣计算器，帮助用户分析优惠券使用策略，计算最优充值方案。

---

## 二、问题修复记录

### 问题1：数额券使用时出现错误
**错误信息**：`couponThreshold is not defined`

**问题原因**：在 `analyzeCoupon` 函数中使用了未定义的变量 `couponThreshold` 和 `couponFixedAmount`，应该使用 `minAmount` 和 `fixedAmount`。

**修复位置**：index.html 第 1595-1596 行和第 1715-1716 行

**修复内容**：
```javascript
// 修改前
if (tier.amount >= couponThreshold) {
  actualPayment = tier.amount - couponFixedAmount;
}

// 修改后
if (tier.amount >= minAmount) {
  actualPayment = tier.amount - fixedAmount;
}
```

---

### 问题2：手机端数额券功能无法使用
**错误信息**：`分析过程中出现错误，请检查控制台日志`

**问题原因**：
1. 输入验证不完善，`parseFloat('')` 返回 `NaN`
2. 金额券模式下剩余金额处理逻辑错误

**修复位置**：index.html 第 958-981 行和第 1720-1721 行

**修复内容**：
```javascript
// 改进输入验证
minAmount = parseFloat(document.getElementById('minAmount').value) || 0;
fixedAmount = parseFloat(document.getElementById('fixedAmount').value) || 0;

// 修复金额券使用逻辑
// 金额券单次使用，使用后剩余金额为0
stepRemainingCoupon = 0;
```

---

### 问题3：自定义方案功能修复
**问题原因**：`calculateCustomPlan` 函数中，金额券模式下 `couponAmount` 获取错误

**修复位置**：index.html 第 1824-1829 行

**修复内容**：
```javascript
let couponAmount = 0;
if (couponType === 'discount') {
  couponAmount = parseFloat(document.getElementById('couponAmount').value) || 0;
} else {
  couponAmount = couponFixedAmount;
}
```

---

## 三、功能修改记录

### 修改1：取消利润差距和折扣差距计算规则
**需求**：取消"当利润相差小于10%且折扣相差大于0.05时选择折扣小的方案"的规则

**修改内容**：始终选择利润最优方案，不再考虑折扣因素

**修改位置**：index.html 最优方案推荐逻辑部分

---

### 修改2：自定义方案功能迁移到模态框
**需求**：将自定义充值方案功能的结果显示从页面嵌入改为模态框显示

**修改内容**：修改 `calculateCustomPlan` 函数，创建模态框显示分析结果

**修改位置**：index.html `calculateCustomPlan` 函数

---

## 四、Git 使用记录

### 初始化仓库
```bash
cd /d "F:\杂\danzai"
git init
git add index.html
git commit -m "添加 index.html 文件"
```

### 连接 GitHub
```bash
git remote add origin https://github.com/zzm12345-dot/danzai-calculator2.git
git push -u origin master
```

---

## 五、代码结构说明

### 主要函数
1. **`analyzeCoupon()`** - 分析优惠券，计算最优充值方案
2. **`calculateCustomPlan()`** - 计算自定义充值方案
3. **`getCoupons(actualPayment, isRebateDay)`** - 根据实际付款计算获得的优惠券数量
4. **`calculate()`** - 基础计算函数

### 优惠券类型
- **折扣券**：按折扣比例减免，可设置可抵金额
- **金额券**：固定金额减免，需达到使用门槛

### 充值档位
- 68元：748蛋币
- 198元：2118蛋币
- 328元：3548蛋币
- 648元：7068蛋币

---

## 六、注意事项

1. **浏览器缓存问题**：修改代码后，移动端可能需要强制刷新才能生效
2. **输入验证**：确保输入的数值有效，避免 NaN 错误
3. **响应式设计**：页面已优化移动端显示，支持触摸操作
4. **GitHub 推送**：如果遇到网络问题，可以尝试切换 SSH 协议

---

## 七、联系方式
如有问题，请联系项目维护者。

---

*归档日期：2026年5月16日*